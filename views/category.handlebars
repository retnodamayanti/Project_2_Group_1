
<div class="d-sm-flex align-items-center justify-content-between mb-4">
  <h1 class="h3 mb-0 text-gray-800">{{category.name}}</h1>
</div>
<div class="row">
{{#each category.inventories as |inventory|}}
{{> inventory-details}}
{{/each}}
</div>